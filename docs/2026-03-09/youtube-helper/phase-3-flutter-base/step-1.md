# Phase 3: Flutter 기본 구조

## 작업 내용
- Flutter 프로젝트 생성 (`flutter create`)
- 의존성 추가: http, provider, shared_preferences, intl, cached_network_image
- 모델 생성: `VideoSummary` (toJson/fromJson)
- 서비스 생성: `ApiService` (transcript, summarize, chat)
- Provider 생성: `VideoProvider` (상태 관리, 히스토리 저장)
- main.dart: MaterialApp + BottomNavigationBar 구성

## 결과
✅
